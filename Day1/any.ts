/* Normaly you don't have to use any coz of any don't cheack any type so that times its create problems and we can't find any problem. So that we don't use any  */

let hero;

function getHero() {
  return "thor";
}
hero = getHero();
console.log(hero);

/* As same way if you use the type that time its make other output */
let theif: string;
function theifGet() {
  return "Murgi chora";
}
theif = theifGet();
console.log(theif);
