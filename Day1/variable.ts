/* At frist any variable decleared as js but after declearing you must be use clone(:) and type and the syntax will be:
let variableName:type=value; 
*/
let nameing: string = "rakibul";

let number: number = 54;
let newnameing = nameing.toUpperCase();
let doWork: boolean = false;
console.log(nameing, number);
console.log(newnameing, doWork);
